import os
import sys
#This is a simple script that will call the OS to open the specified file, in this case an audio file
#Replace "--" with audio file name
os.startfile("--")

#Program closes after specified duration() 
sys.exit(7)

#To use this script on startup;
#Press Win + R to open Run window
#Type "shell: startup" and press enter (remove quotes)
#Copy and paste .py file into startup folder
